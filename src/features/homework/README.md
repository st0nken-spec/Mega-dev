# Homework lane ownership

The Homework lane owns this directory and Homework-specific tests. It may
add its route/screen entry through the shared shell (`App.tsx`,
`Home.tsx`) in a small integration commit, but must not edit Play-hub,
Create-hub, or `game.ts` modules.
