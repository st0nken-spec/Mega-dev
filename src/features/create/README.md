# Create lane ownership

The Create-hub lane owns this directory and Create-specific tests. It may add its route/screen entry through the shared shell in a small integration commit, but must not edit matching-game domain or play-engine modules.
