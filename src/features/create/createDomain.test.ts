import { describe,expect,it } from 'vitest'
import { coloringPages,nextRegionColor,palette,traceGuides } from './createDomain'
describe('create content',()=>{it('offers varied safe offline activities',()=>{expect(coloringPages.length).toBeGreaterThanOrEqual(3);expect(traceGuides.map(item=>item.symbol)).toEqual(expect.arrayContaining(['○','△','A','5']));expect(palette.length).toBeGreaterThanOrEqual(6)});it('toggles a filled region back to white',()=>{expect(nextRegionColor(undefined,'red')).toBe('red');expect(nextRegionColor('red','red')).toBe('#ffffff')});it('has unique page and region ids',()=>{expect(new Set(coloringPages.map(page=>page.id)).size).toBe(coloringPages.length);for(const page of coloringPages)expect(new Set(page.regions.map(region=>region.id)).size).toBe(page.regions.length)})})
